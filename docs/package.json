{
  "name": "docs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "deploy": "vitepress build && wrangler pages deploy",
    "preview": "vitepress build && wrangler preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "medium-zoom": "catalog:",
    "vitepress": "catalog:",
    "vue": "catalog:"
  },
  "devDependencies": {
    "oxc-minify": "catalog:",
    "vite": "catalog:",
    "vitepress-plugin-llms": "catalog:",
    "wrangler": "catalog:"
  }
}
