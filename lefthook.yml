pre-commit:
  commands:
    lint:
      glob: "*.{ts,vue}"
      run: "pnpm exec eslint --fix {staged_files}"
      stage_fixed: true
    typecheck:
      glob: "*.{ts,vue}"
      run: pnpm run typecheck
commit-msg:
  commands:
    commitlint:
      run: "pnpm exec commitlint --edit {1}"
